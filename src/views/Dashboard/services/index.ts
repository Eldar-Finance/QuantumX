export const sendUserTokens = async (
  address: string,
  token: {
    amount: string;
    identifier: string;
    decimals: number;
  },
  fee: string,
  data: string
) => {
  if (token.identifier === "EGLD") {
  } else {
  }
};
